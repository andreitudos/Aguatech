@model Aguatech.Web.Data.Entities.Marca

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Marca</h4>
    <hr />
    <div>
        @if (!string.IsNullOrEmpty(Model.ImageUrl))
        {
            <img src="@Url.Content(Model.ImageUrl)" alt="image" style="width:auto; height: 250px; max-height:250px;" />
        }
        else
        { <img src="~/images/download.png" alt="image" style="width:100px; height: auto; max-width:100%;" />}
    </div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
       
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Edit</a>
    <a asp-action="Index" class="btn btn-success">Back to List</a>
</div>
